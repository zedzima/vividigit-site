name = "vividigit-checkout"
main = "worker.js"
compatibility_date = "2024-09-01"

# Environment variables — set via Cloudflare dashboard or `wrangler secret put`:
#   REVOLUT_API_KEY  — Revolut Merchant API secret key
#   REVOLUT_ENV      — "sandbox" or "production" (default: sandbox)
#   ALLOWED_ORIGIN   — e.g. "https://vividigit.com"
